---
# vars file for k8s-Worker

---
# vars file for k8s-Master

packages:
       - kubeadm
       - kubelet
       - kubectl
       - docker
       - iproute-tc 

services:
      - docker
      - kubelet

